import React from 'react';
import './Button.css';

const clear = [clear];

const Clear = props => {
    return (
        <div>
            <button>{props.actionArray}</button>
        </div>
    )
}

export default Clear;